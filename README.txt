# Simulate-pipline-archeticture-
Compilation and usage Instructions:
1. Navagate to the directory of the source files using cd
2. Type gcc p3.c into the shell.
3. To run the  type command ./a.out file_name.out
Notes: Because there is no fowarding in simulator there ar very bizarre outputs when pipeline instructions cause data harzards. If the instruction data requirement do not overlap in the test file there should be no issues in the output.